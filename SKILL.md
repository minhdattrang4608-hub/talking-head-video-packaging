---
name: 口播视频包装
description: 使用 ChatCut 网页端，在新建的空白独立工程中完成单人口播视频包装。先确认是否抠图；删除非说话空档和气口，保留完整发音；A-roll 位于最高视频画面层，B-roll 和字幕不遮挡脸部。支持字幕、章节动效、进度条与可选配乐、横版和竖版。
---

# 口播视频包装

## 使用场景

适用于单人口播视频的完整包装，博主/内容创作者风格，支持 16:9 横版（1920×1080）与 9:16 竖版（1080×1920）。输入可以是原始口播视频（走完整流程），也可以是已完成 A-roll 粗剪的时间线（不重做内容取舍，但仍须检查和删除残留的非说话空档）。

典型触发词：「帮我做口播包装」「口播剪完了，加字幕/背景/B-roll/进度条」「按照我的口播模板包装一下」

## 执行入口：ChatCut 网页端与独立工程

- 默认在可控浏览器中打开 **ChatCut 网页端**：`https://app.chatcut.io/`。用户提供其他已确认的网页入口时使用其网址。核实域名、登录账号和工作区后进入编辑器；页面提示下载或唤起客户端时，不跟随打开桌面应用。
- 优先直接操作网页编辑器。只有实际工具文档明确支持当前网页会话和目标工程时才使用连接工具；不得把仅供 ChatCut Desktop 使用的 MCP、CLI 或本地工程路径当作网页连接。工具名、参数和功能以实际文档或页面为准，不虚构 API，不假定桌面端功能在网页端全部可用。
- 不因 ChatCut 桌面客户端、剪映或 CapCut 已在前台、已有工程打开，或自动化工具更方便，就切换到它们。只有用户明确指定其他客户端或编辑器时才改变选择。
- 网页端无法访问、未登录或缺少必要能力时，说明具体阻塞和恢复方式；不得静默启动桌面客户端、改用剪映或编辑旧工程。需要登录时由用户完成登录，再核实工作区。
- 每个新的视频包装任务默认**新建独立工程**。只有用户明确指定继续某个既有工程时才复用；应用当前打开、最近使用、列表第一项、名称相似，都不是复用依据。操作系统项目目录也不等于 ChatCut 当前工程。
- 在首次导入或修改前，确认并记录浏览器标签页、工程名称、工程 URL、工程 ID（页面不提供时使用可核对的唯一标识）、目标时间线及素材来源。后续操作都绑定这个目标；支持显式 ID 的工具必须传入已核实的目标 ID。
- 切换标签页、切换工程、页面刷新、连接恢复或任务续接后，先重新核对目标工程与时间线再写入。身份不匹配或无法核实时停止写入，定位已记录的工程，不选择最近工程代替。续接本次任务时使用已记录的新工程，不重复新建。

### 网页导入与积分限制

- 导入本地视频使用网页的「上传 / 导入媒体」控件；浏览器工具支持文件上传时直接选择已核实的绝对路径。若只能使用系统文件选择器，优先用「前往文件夹」定位父目录再选文件（macOS 为 Command+Shift+G），不要反复逐目录导航。网页无法直接读取用户消息中的本地路径，不向内置 AI 对话框发送路径来代替上传。
- 上传后确认文件名、视频时长、缩略图和处理状态，再加入时间线。浏览器上传能力确实不可用时才请求用户完成上传；不因为 AI 发送按钮禁用就让用户接管文件导入。
- 内置 AI Agent 的积分不足仅说明相应 AI 操作受限；先检查普通上传和编辑控件是否可用。普通功能可用就继续；抠像、生成素材等若也受积分限制，记录该步骤为阻塞，不反复发送、自动购买积分或宣称网页端能绕过额度。
- 新任务不继承旧工程的模板或失联素材。章节标题、进度条、背景图仅使用本次任务已验证可用的素材；需要模板时另行导入或制作。仅在用户明确要求修复或继续源工程时，才处理其「重新关联」提示。

## 0. 输入澄清

拿到视频后先核对本次视频已有的用户答复，一次询问缺失项，并等待明确回答后再动手。不重复询问本次视频已明确回答的选项，不把其他视频的偏好当成本次答复：

1. 画幅：16:9 横版 / 9:16 竖版？
2. 是否需要抠图换背景：要 / 不要（保留原背景）？
3. 若需要抠图，使用用户提供的背景图，还是使用米白背景（须确认）？
4. 是否需要背景音乐：要（生成 1 首候选并确认）/ 不要（跳过，A1 留空）？

**是否抠图是必须确认的前置条件，没有默认值。** 本次视频尚未得到明确答复时，必须直接提问：「这条视频是否需要人物抠图并更换背景，还是保留原背景？」“帮我包装”“按模板做”“继续”、用户沉默和问题框预选值都不等于答复。没有回答时可以检查文件和网页能力，但不得创建制作工程、导入、精剪、抠像或布置包装；等待用户回答，不替用户决定。

记录本次视频的选择为「抠图：要 / 不要」及对应用户答复，并在开始制作前简短复述。选「要」才进入抠像和换背景；选「不要」则保留原背景，跳过全部抠像步骤，不为了露出 B-roll 自动开启抠像。画幅、背景和 BGM 也使用已确认的选择；米白背景仅在已确认换背景且未提供背景图时作为建议供用户确认。

## 需要的输入

- 原始口播视频，或已完成 A-roll 粗剪的时间线
- （可选）用户确认抠图后提供的背景图，或其确认使用的米白背景
- 米白背景图（用户确认使用时准备，不假定新工程素材库已有）
- 背景底图模版（用于了解布局：素材区、人物区、字幕区位置）
- B-roll 配图（按章节命名，如 skills1-xxx、01-湖景插画 等）
- 已沉淀的章节进度条 MG（核实可用后导入，支持章节帧数参数；没有时另行制作）
- 章节起始帧列表（从转录/时间线中读取）

## 画面布局

以下坐标仅是画幅布局参考，不能覆盖「A-roll 最高视频画面层」规则。人物抠像须有用户明确同意；不抠像时保留原背景，在独立人物区域显示完整 A-roll。B-roll、标题和进度条使用人物区域以外的空间；若 A-roll 铺满画布导致下层不可见，先提出缩放或分区方案供用户确认，不把 B-roll 移到人物上层，不隐藏 A-roll。

### 9:16 竖版（1080×1920）

```
┌─────────────────────┐  ← 顶部：章节进度条
│  章节进度条 (top:0)  │
├─────────────────────┤  ← B-roll 区：顶部 1/4（height≈480, top:0）
│   B-roll 配图区      │     素材等比适配该区域，不溢出到人物区
├─────────────────────┤
│                     │  ← 人物区：下 3/4（top:480, height:1440）
│   人物（A-roll）     │     仅经用户同意才去除原背景
│                     │     叠在背景图上
├─────────────────────┤  ← 字幕区：底部仅示意，实际位置必须避脸
│       字幕           │
└─────────────────────┘
```

### 16:9 横版（1920×1080，同比例映射）

- B-roll 区：顶部 1/4（height≈270, top:0），图按画布宽度适配。
- 人物区：下方 3/4（top:270, height:810）。
- 字幕区：按实际脸部位置确定的安全区，底部仅为候选位置，须满足第 5 步的避脸要求。
- 各元素尺寸按竖版同比例换算（示意参数，可按用户模板微调）。

## 工作流步骤

### 1. 输入澄清

执行「0. 输入澄清」。先检查是否有本次视频「抠图：要 / 不要」的明确答复；缺失时提问并等待，不执行第 2 步及后续制作。确认画幅、抠图、背景、BGM 后再开始。

### 2. 在 ChatCut 网页端新建并核实空白工程（必须先完成）

1. 按「执行入口」打开 ChatCut 网页端。若页面进入旧工程，保留原标签页和未保存工作，从网页工作区的「新建工程 / 空白工程」入口创建；不使用现成模板工程，不清空旧时间线冒充新工程。
2. 工程名称默认用「素材名或主题-口播包装-YYYYMMDD-HHmmss」，重名时添加序号，禁止覆盖。已获授权的视频包装任务直接新建，无需再次询问是否新建。
3. 从创建结果和页面核实新工程身份、URL、已选中状态及初始空时间线，记录目标标识；设置已确认的画幅。首次导入前，时间线上应没有片段和既有成片时长，工程素材区应没有已导入的旧素材或「点击重新关联」条目（平台公共素材目录不计）。名称包含「新片」不代表新工程；若已存在成片、模板片段或失联素材，立即停止导入和编辑，返回空白新建入口，不修复这些旧素材。创建请求超时或结果不明确时，先查证是否创建成功，不盲目重试或打开相似旧工程。
4. 只有工程身份核实通过后，才允许导入本次任务的素材。素材优先来自用户指定文件或当前项目已明确属于本次任务的文件，不从旧工程素材库随意挑选。

用户明确要求继续某个既有工程时，按其提供的名称、路径或 ID 精确定位并核实；不能唯一定位时只补问目标，不猜测。此分支仍须先完成下一步的时间线复制。

### 3. 导入与复制时间线（任何剪辑前完成）

- 原始视频：按「网页导入与积分限制」上传到已核实的目标工程，建立「原始素材」时间线，再复制为「包装版」。
- 已有粗剪：只读取用户明确指定的源工程和源时间线，通过 ChatCut 实际支持的复制或导入方式放入新工程，保留一份粗剪基线，再创建「包装版」副本。不能跨工程复制时，说明缺失能力并索取可导入材料，不自动回到源工程编辑。
- 用户明确要求在既有工程继续时，也先复制其指定时间线为「包装版」，不修改源时间线。
- 核实副本与基线是两个独立时间线，记录副本 ID 或唯一标识，并将副本设为当前目标。后续抠像、精剪、字幕和包装都只操作该副本。
- 若网页端不支持复制时间线，使用已验证的「复制工程」功能保留基线并创建独立包装版，记录两个工程 URL；两种复制能力都不可用时，说明阻塞，不假称副本已创建，也不转去桌面端继续。

### 3.5 AI Agent 自然语言驱动模式（推荐）

完成输入确认、工程新建、视频上传和包装版副本核实后，AI Agent 可用时优先通过网页 AI 对话框分步驱动字幕转写、口播清理、B-roll 配图、章节进度条等操作。内置 AI 同样必须遵守本 Skill 的气口、图层和字幕避脸规则；积分不足时按「网页导入与积分限制」处理。

**指令模板示例**（先用本次已确认的选择替换方括号，不把示例当作用户答复）：
```
只操作已核实的包装版副本。画幅：[已确认画幅]；抠图：[用户明确选择]；背景：[已确认背景或保留原背景]；背景音乐：[用户明确选择]。
若用户已确认需要抠像，先按第 4 步完成整条原始视频的抠像，再精剪分段；若不需要则保留原背景。
先转写并清理口播：删除首尾、句内、句间的全部非说话空档及独立呼吸，闭合音画时间线，不默认保留 0.25 秒停顿，保护完整字头、尾音和轻声。完成后报告清理前后时长，先检查残留空档及所有接缝，再进行后续包装。
按最终语音时间轴生成字幕：青松手写体、白字黑阴影，72px 仅作试排起点。按人物脸部移动范围预留禁入区，所有字幕、关键词放大和动画都避开脸；长句拆分并调整字号和位置。
添加 B-roll 和章节进度条时，A-roll 始终在最高视频画面层，配图及动效都在其下方。不得用 B-roll 覆盖人物。
```

**AI 自动完成的工作**：
- 字幕转写与口播清理：去除口癖、非说话空档和重复词，按第 4 步复查，不把 AI 的完成提示当作试听通过。
- 字幕样式设置：青松手写体、白色字体黑色阴影；按第 5 步实际避脸区域调整字号和位置。
- B-roll 配图：按章节语义和已确认的素材偏好匹配网络图片或用户素材，放在 A-roll 下方。
- 章节进度条：识别章节，并提供风格选项，章节帧必须基于气口清理后的最终时间线。

**交互要点**：
- AI 完成每一步后可能提供 B-roll 素材偏好、进度条风格等选项；依据已确认需求选择，未确定时向用户澄清，不让这些选项代替前面的抠图确认。
- B-roll 选项可能包括：「我有 B-roll 素材，稍后上传」「帮我找网络图片配 B-roll」「B-roll 先跳过，直接做章节进度条」「B-roll 和章节进度条一起规划」。
- 进度条风格选项可能包括：「简洁细线条，白色或浅色系」「彩色分段块，每章不同颜色」「极简点状进度条」「科技感暗色风格」。
- AI 完成后独立核验气口、字幕避脸和图层顺序；有问题时用具体时间段和修正要求继续调整，不仅检查 AI 主动报告的问题。

**注意事项**：
- 视频必须先通过上传控件导入素材库，再在 AI 对话框中用 @ 引用或描述已上传素材。
- 不要在 AI 对话框中直接输入本地文件路径代替上传。
- AI 处理期间发送按钮可能变成「停止」，等待其完成后再进行下一步操作。

### 4. 人物抠像与 A-roll 精剪（仅在包装版副本中）

**进入本步骤前重新核对本次用户答复：未确认则返回第 0 步提问；明确选择不抠像则跳过以下抠像操作，保留原背景，只进行需要的精剪。明确选择抠像时，先对副本中的整条原始视频抠像，再精剪分段，子段继承抠像结果。**

1. 先核实网页端支持人物抠像且账号可用，再对副本中的原始整条视频添加 1 次效果并等待处理完成。保持目标标签页打开；仅当网页连接工具支持效果查询时分批查询，每次 ≤20 个 effect ID，否则查看页面处理状态。
2. 抠像完成后进行 A-roll 精剪分段。
3. 验证继承：抽帧确认所有子段背景透明；若有子段未继承 → 降级：仅对缺失段补挂效果，每批 ≤10 个。

输入已是粗剪时间线且需要抠像时：对每段添加网页端可用的人物抠像效果，分批处理（每批 ≤10 个，完成后再处理下一批），保持目标标签页打开。

原始视频的精剪遵循 talking-head-guide 的完整语义和发音保护原则：转写、机械清理、语义精剪、检查结果；**气口处理采用下面的无空档规则，覆盖该指南默认保留约 0.25 秒停顿、保留正常呼吸或情绪停顿的建议。** 仅当工具明确连接网页目标工程时使用 clean_script / apply_script；否则使用网页实际提供的文字稿或时间线编辑功能，不为获得桌面专用工具切换客户端。输入已经是粗剪时间线时不重做内容取舍，但必须执行以下气口清理与验收。

#### 气口与非说话空档：删除，不只压缩

- 默认删除口播中全部可独立剪除的非说话区间，包括片头开口前、片尾说完后、句间和句内停顿、独立吸气吐气、等待与重录间隙；目标保留时长为 0，不统一留 0.25 秒，不以“自然节奏”保留长气口。仅当用户明确指定保留某处停顿时例外。
- 结合人声轨的实际音频、波形、可用的语音活动检测及文字时间戳定位区间。不能只按转写段落首尾剪：一个转写段内也可能有长空档；背景音乐或环境噪声有能量不代表人在说话。检查气口时单独听人声，不凭混音响度判定。
- “无说话”不是“没有文字”或“低音量”。轻声、清辅音、字头、尾音、音节内部的闭塞段仍属于完整发音，不能按静音阈值机械切掉。对候选空档听辨边界，贴近最后发音结束和下一次发音开始剪；仅保留不切断发音所必需的极短边界余量，不把它扩成固定气口。
- 对 A-roll 音画同步做波纹删除并闭合时间线；只静音、删字幕、删音频却保留视频时长，或留下空白片段，都不算剪掉。工具只支持压缩时，检查其实际最小保留值，继续用网页时间线修掉残留；不把一次自动清理返回成功当作验收通过。
- 先完成气口清理，再生成字幕、安排 B-roll、章节帧和配乐。修复已有包装版时，同步重排后续素材、重算章节帧并重新对齐字幕，保持音画同步，不能留下旧时间戳或新增黑帧。
- 清理后重新扫描整条人声，复查全部残留非说话候选、所有剪切接缝及首尾，并完整播放试听一次。检查漏剪、吞字、爆音、重复字和音画错位；修复接缝时使用编辑器支持的短淡化等方式，不重新塞回长停顿。记录清理前后时长、删除空档总时长及用户指定的保留例外。无法听辨或检查全片时，明确标为未完成验证，不能声称全部气口已清理。

**卡住处理 SOP**：效果处理超过约 20 分钟无进展时，先查看页面错误、网络连接和积分状态。额度或能力不足时报告具体阻塞；普通页面异常时，确认工程已保存后刷新目标网页，再核实 URL、工程和包装版时间线。刷新后先检查原任务结果，避免重复提交付费任务；持续失败时保留工程并报告，不重启桌面客户端或无限重试。

### 5. 字幕：先确定不挡脸的区域，再排版

- 开启字幕，默认使用「青松手写体（Qingsong Shouxie Ti）」（ChatCut 字体库内置；如用户另有指定则按用户指定）。
- 字号 72px 仅为 1080 宽画布的试排起点，不能固定套用；按画幅、实际字幕区和移动端可读性调整。字间距：1，白色字体，黑色阴影 85% 强度，无描边。
- 关键词红色高亮（`#FF3333`，fontWeight:700）：根据内容语义判断，典型如技术术语、产品名、核心卖点词。
- 以精剪后的最终时间线生成或重新对齐字幕，不能沿用清理气口前的时间戳。
- 按最终裁剪、缩放和人物位置，检查每个镜头中脸部随时间移动的范围，建立包含额头、眼鼻、嘴和下巴的禁入区域。向四周增加至少画布短边 3% 的余量（1080 短边约 32px）；这是最小留白，人物移动更大时继续扩大。不以首帧或模板坐标代表全片。
- **字幕完整可见范围不得与脸部禁入区域相交**，包括第二行、关键词放大、描边/阴影、底板和入退场动画。独立字幕层并不自动安全，必须检查最终合成画面。
- 优先选择脸部下方的胸前或画面底部留白区，同时遵守发布平台的界面安全区；没有平台模板时先在画布边缘内留至少 5% 作为试排边距，再检查实际显示。不因“已经放到底部”就跳过避脸检查。
- 长句优先按语义拆成连续短字幕，每条最多两行，保证每个字可读且完整落在安全区；必要时调整字号、行距和位置。空间仍不足时使用其他无脸留白区，或提出并确认人物布局调整。禁止通过极小字号、裁掉字幕、遮住脸或隐藏字幕来凑合通过。
- 同一镜头优先使用稳定字幕位置；镜头或人物位置变化时单独调整该段，不能只移动第一条字幕就认为全部完成。首次试排先检查最长双行字幕、人物最低头位置及最大关键词状态，再应用全片。
- 验收须覆盖每条字幕显示期间的脸部位置和字幕动画：逐条检查进入、中段、退出，对人物运动或动画区间连续播放，完整预览全片。发现交叠先移动、拆句或调整尺寸，再复查；不能只抽查章首章尾。

### 6. 画面层搭建（轨道顺序从下到上）

**A-roll 必须位于所有视频画面轨道的最上层。** 以下 V1–V5 是合成层级示意，实际网页轨道编号可能不同，须通过画面预览确认遮挡关系，不能只看轨道名称。字幕使用独立字幕层，在安全区显示，避免遮挡脸部；B-roll、MG 标题和进度条都不能占用 A-roll 上方的视频画面层。

| 轨道 | 内容 | 说明 |
|---|---|---|
| V1 背景 | 用户确认的背景图 | 需要换背景时全程铺底；不抠像时保留 A-roll 原背景 |
| V2 B-roll | 各章节配图 | 位于 A-roll 下方，使用预留配图区，不遮挡人物 |
| V3 MG标题 | 章节标题 MG | 位于 A-roll 下方，使用人物区域外的标题区；每章节开场约 2 秒 |
| V4 章节进度条 | 进度条 MG | 位于 A-roll 下方，使用预留进度条区域 |
| V5 A-roll（最高视频画面层，anchor） | 口播视频及人声 | 全程保持在最上层；仅用户同意时抠像，否则保留原背景 |
| 字幕层 | 口播字幕 | 独立字幕层，放在安全区，不遮挡脸部 |
| A1 背景音乐（follower） | BGM | 仅当输入澄清选择要 BGM；A-roll 人声设 anchor，A1 设 follower 自动闪避 |

新增素材后检查实际合成顺序：若编辑器自动把新素材放到最高层，立即移到 A-roll 下方并预览。不得以“B-roll 需要看见”为由降低、隐藏、替换或裁掉 A-roll；出现空间不足时调整下层素材或使用已确认的分区布局。

### 7. B-roll 配图

- 根据语义在各章节关键时刻放置配图，每处约 3–4 秒。
- 尺寸位置按实际预留配图区计算，保持素材比例，完整放在该区域内；不机械沿用可能侵入人物区域的旧模板坐标。
- 所有 B-roll 位于 A-roll 下方，与人物同时可见；不得使用全屏覆盖、替换口播片段或画中画上层遮住人物。
- 每段 B-roll 放置后预览其进入、中段、退出，包含运动和转场过程，确认人物仍在最前景、没有被遮挡；修正后再继续批量放置。

#### 布局调整常见问题

AI 自动生成的 B-roll 布局可能不符合规范，需要手动调整。常见问题及解决方案：

- **人物不可见**：预览区只显示 B-roll，没有显示人物。原因是 B-roll 全屏覆盖了 A-roll。解决方案：在 AI 对话框中输入"调整布局让人物完整显示在下方，B-roll 在顶部区域"，AI 会自动调整所有片段的位置和大小（通常调整 20+ 个片段）。
- **画幅不对**：AI 可能不会自动设置正确的画幅。解决方案：手动点击「画幅比例」按钮，选择 9:16（竖版）或 16:9（横版）。
- **开头 B-roll 黑屏**：第一张配图可能因为图片加载问题没有显示。解决方案：替换成其他图片，或等云端渲染完成后再确认。
- **部分段落 B-roll 不显示**：个别段落的 B-roll 图层可能没有正确渲染。解决方案：拖动播放头到对应时间段，确认 B-roll 是否显示，必要时重新放置。

**正确布局参考（9:16 竖版 1080×1920）**：
- 顶部细条（约45px）：章节进度条
- 上半区（45-920px）：B-roll 配图（全屏填充）
- 下半区（1035-1845px）：人物口播，完整显示
- 字幕区：底部安全区，避开脸部

### 8. 章节 MG 标题

- 每进入新章节时显示标题，时长约 2 秒，放在 V3 轨道、A-roll 下方的预留标题区。
- 极简细线风格：深色文字、细横线装饰、淡入淡出。
- 首个 MG 经确认后，后续章节复用其视觉系统，保持一致。

### 9. 章节进度条

- 调用「进度条 Skills」制作或复用章节进度条。
- 确认章节帧属性与当前时间线章节起始帧一致。
- 进度条覆盖全程，放在 V4、A-roll 下方的预留区域，并根据实际字幕和安全区确定位置；不得占用最高视频画面层。

### 10. 背景音乐（可选）

- 仅当输入澄清中用户选择要 BGM。
- 轻快、博主风、无人声、acoustic + electronic 混合，不分散注意力。
- 覆盖视频内容全长，超出部分截断，添加淡入淡出各 2 秒。
- 人声轨设为 anchor，音乐轨设为 follower，自动在人声下方闪避。

### 11. 验证

- 核对当前应用、工程和包装版时间线与记录一致，源工程、基线时间线均未被修改。
- 核对本次用户抠图答复与实际效果一致：选「不要」时原背景仍在；选「要」时检查抠像完成且各子段继承正确。
- 按第 4 步对整条人声执行非说话空档复查和完整试听，确认首尾、段内、段间都无遗漏，剪口不吞字，音画同步；粗剪输入也必须检查。
- 结构核对为主：用网页时间线检查轨道、条目数、图层位置；preview_timeline 仅在实际支持当前网页工程时使用。
- 检查全部 A-roll 子段都在最高视频画面层，并核验每段 B-roll 的进入、中段、退出及转场，人物始终在最前景；不能只抽查章节边界而漏掉章节中间的遮挡。
- 按第 5 步检查每条字幕的实际包围范围及人物移动，确认文字、阴影和动画都在脸部禁入区域之外，且与精剪后语音对齐。
- 每章首尾各 1 帧和最后一帧用于补充检查抠像、章节标题和进度条，不能替代全片气口试听及逐条字幕避脸检查。
- 不高频调用 viewer；全部合格后再导出。

### 12. 导出

- 从已核实的目标工程和包装版时间线导出，不使用未经核实的当前活动时间线。
- 按确认的画幅导出（竖版 1080×1920 / 横版 1920×1080）。
- 导出流程：点击右上角「导出」按钮 → 选择视频标签页 → 确认格式为 MP4 (H.264) → 选择分辨率（推荐 1080p）→ 选择帧率（推荐 30fps）→ 点击「导出」按钮开始渲染。
- 渲染完成后视频会自动下载到本地下载文件夹，文件名格式为「工程名 + 分辨率.mp4」。
- 若浏览器拦截了自动下载，点击导出面板中的「再次下载」或「查看」按钮手动下载。
- 导出后完整播放检查气口、音画与字幕同步，复查长字幕、人物运动及字幕动画区间的避脸情况；确认后交付文件。

## 关键规则

- 保留完整口播语义和发音，删除非说话空档；"不删口播内容"不代表保留气口。包装阶段如发现漏剪，回到 A-roll 修复并同步更新后续素材时序。不得用 B-roll 替换、隐藏或覆盖 A-roll；A-roll 在最高视频画面层，所有配图和视频动效位于其下方。
- 字幕避脸优先于固定字号和模板坐标，所有文字装饰及动画范围都必须避开脸部。
- 抠像必须有本次用户明确答复；仅当选择抠像且输入为原始视频时，先抠像后精剪，子段未继承则降级补挂，每批 ≤10。
- 进度条帧数对齐：章节帧必须与实际章节起始帧一致。
- BGM 不超过视频结尾：音乐不能延长时间线。
- 抠像需要等待：AI 处理期间不要反复重试；保持 ChatCut 网页目标标签页打开。
- 效果状态查询每次 ≤20 个 ID；卡住按 SOP 处理。
- B-roll 尺寸统一：所有配图沿用同一位置和尺寸系统。
- 优先使用可用的 AI Agent 自然语言驱动模式分步完成包装；必须把气口删除和字幕避脸要求传入指令，AI 完成后仍须独立检查。
- 视频素材必须通过上传控件导入，不能在 AI 对话框中输入本地路径代替。
- 浏览器自动化操作时，DOM ref 会过期，每次操作前需重新 snapshot 获取最新 ref。
- AI 自动生成的布局可能不符合规范，必须手动检查并调整：确认人物完整显示、B-roll 在顶部区域、画幅比例正确。

## 完成检查

- [ ] 使用 ChatCut 网页端及已核实的账号、工作区；没有自动打开桌面客户端或剪映。
- [ ] 新任务已新建并核实独立工程；复用旧工程仅限用户明确指定。
- [ ] 首次导入前确认空白时间线和无旧素材，不靠工程名称判断新建成功。
- [ ] 视频通过上传控件导入并核实，不依赖内置 AI 对话框读取本地路径；积分受限步骤如实记录。
- [ ] 目标工程、包装版时间线及素材来源有记录，所有写入与导出均指向该目标。
- [ ] 在抠像与精剪前已创建独立包装版副本，旧工程及源时间线未被误改。
- [ ] 输入澄清已确认（画幅/抠图/背景/BGM）。
- [ ] 已记录本次用户关于抠图的明确答复；没有用默认值、沉默或泛化的"继续"替代回答。
- [ ] 首尾、段内、段间的非说话空档与独立气口已删除并闭合时间线；无默认保留的 0.25 秒停顿，用户指定例外已记录。
- [ ] 整条人声已复查并完整试听，无漏剪、吞字、爆音、黑帧及音画错位；已记录清理前后时长。
- [ ] 字幕使用确认的字体，字号按实际安全区调整，关键词高亮覆盖核心词。
- [ ] 每条字幕含双行、关键词、阴影、底板和动画的完整范围均不进入脸部禁入区域；人物移动区间已播放验证。
- [ ] 字幕、B-roll、章节帧和配乐已与气口清理后的最终时间线重新对齐。
- [ ] 用户选抠像：效果完成且所有段背景透明；用户选不抠像：未添加抠像效果、原背景保留。
- [ ] 进度条章节帧数与时间线一致。
- [ ] B-roll 所有图片位置一致，没有偏移。
- [ ] A-roll 全部子段处于最高视频画面层；每段 B-roll 的进入、中段、退出及转场均已检查，无人物遮挡。
- [ ] 章节 MG 标题视觉风格一致。
- [ ] 背景音乐（如有）在人声段落自动压低。
- [ ] 原始时间线未被修改，副本独立可编辑。
- [ ] 按确认画幅导出，成片完整播放及字幕避脸检查通过。
- [ ] 布局已手动检查并调整：人物完整显示在下方，B-roll 在顶部区域，画幅比例正确。
