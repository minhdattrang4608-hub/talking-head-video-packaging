---
name: 口播视频包装
description: 使用 ChatCut，在新建的独立工程中完成单人口播视频包装。适用于原始视频或用户指定的粗剪时间线，包含精剪、抠像、字幕、B-roll、章节动效、进度条与可选配乐，支持横版和竖版。默认不使用剪映或复用旧工程。
---

# 口播视频包装

## 使用场景

适用于单人口播视频的完整包装，博主/内容创作者风格，支持 16:9 横版（1920×1080）与 9:16 竖版（1080×1920）。输入可以是原始口播视频（走完整流程），也可以是已完成 A-roll 粗剪的时间线（跳过精剪，直接从抠像/包装开始）。

典型触发词：「帮我做口播包装」「口播剪完了，加字幕/背景/B-roll/进度条」「按照我的口播模板包装一下」

## 执行入口：ChatCut 与独立工程

- 默认使用 **ChatCut**。先发现当前环境实际提供的 ChatCut 工具或连接入口；原生工具可用时优先使用，否则可以操作 ChatCut 界面。工具名、参数和工程能力以实际工具文档或界面为准，不虚构 API。
- 不因剪映、CapCut 已在前台、已有工程打开，或自动化工具更方便，就切换到它们。只有用户明确指定其他编辑器时才改变应用选择。
- ChatCut 无法启动、连接或缺少必要能力时，说明具体阻塞和恢复方式；不得静默改用剪映，也不得通过编辑旧工程绕过新建工程步骤。
- 每个新的视频包装任务默认**新建独立工程**。只有用户明确指定继续某个既有工程时才复用；应用当前打开、最近使用、列表第一项、名称相似，都不是复用依据。操作系统项目目录也不等于 ChatCut 当前工程。
- 在首次导入或修改前，确认并记录目标工程名称、工程 ID（界面不提供 ID 时记录可核对的工程路径或唯一标识）、目标时间线及素材来源。后续操作都绑定这个目标；支持显式 ID 的工具必须传入已核实的目标 ID。
- 切换窗口、切换工程、应用重启、连接恢复或任务续接后，先重新核对目标工程与时间线再写入。身份不匹配或无法核实时停止写入，定位已记录的工程，不选择最近工程代替。续接本次任务时使用已记录的新工程，不重复新建。

## 0. 输入澄清

拿到视频后先一次问清，问完再动手，不默认、不猜：

1. 画幅：16:9 横版 / 9:16 竖版？
2. 是否需要抠图换背景：要 / 不要（保留原背景）？
3. 若需要抠图，背景图是否已提供：已提供（用用户提供的背景图）/ 未提供（用默认米白背景）？
4. 是否需要背景音乐：要（生成 1 首候选并确认）/ 不要（跳过，A1 留空）？

默认值（用户不明确时）：9:16 竖版 + 抠图 + 米白背景 + 无 BGM。

## 需要的输入

- 原始口播视频，或已完成 A-roll 粗剪的时间线
- （可选）用户提供的背景图；未提供时用默认米白背景
- 米白背景图（默认，项目素材库中）
- 背景底图模版（用于了解布局：素材区、人物区、字幕区位置）
- B-roll 配图（按章节命名，如 skills1-xxx、01-湖景插画 等）
- 已沉淀的章节进度条 MG（从项目素材库调用，支持章节帧数参数）
- 章节起始帧列表（从转录/时间线中读取）

## 画面布局

### 9:16 竖版（1080×1920）

```
┌─────────────────────┐  ← 顶部：章节进度条
│  章节进度条 (top:0)  │
├─────────────────────┤  ← B-roll 区：顶部 1/4（height≈480, top:0）
│   B-roll 配图区      │     4:3 横版图，width:1080，按比例显示
├─────────────────────┤
│                     │  ← 人物区：下 3/4（top:480, height:1440）
│   人物（已抠像）     │     AI Portrait Cutout 去除原背景
│                     │     叠在背景图上
├─────────────────────┤  ← 字幕区：底部（参考底图模版位置）
│       字幕           │
└─────────────────────┘
```

### 16:9 横版（1920×1080，同比例映射）

- B-roll 区：顶部 1/4（height≈270, top:0），图按画布宽度适配。
- 人物区：下方 3/4（top:270, height:810）。
- 字幕区：底部安全区（参考底图模版位置）。
- 各元素尺寸按竖版同比例换算（示意参数，可按用户模板微调）。

## 工作流步骤

### 1. 输入澄清

执行「0. 输入澄清」，确认画幅、抠图、背景、BGM 后再开始。

### 2. 在 ChatCut 新建并核实工程（必须先完成）

1. 按「执行入口」连接或打开 ChatCut。若启动后进入旧工程，先保留其未保存工作，再通过 ChatCut 的工程入口新建；不得清空旧时间线冒充新工程。
2. 工程名称默认用「素材名或主题-口播包装-YYYYMMDD-HHmmss」，重名时添加序号，禁止覆盖。已获授权的视频包装任务直接新建，无需再次询问是否新建。
3. 从创建结果或界面核实新工程身份、已选中状态及初始空时间线，记录目标标识；设置已确认的画幅。创建请求超时或结果不明确时，先查证是否创建成功，不盲目重试或打开相似旧工程。
4. 只有工程身份核实通过后，才允许导入本次任务的素材。素材优先来自用户指定文件或当前项目已明确属于本次任务的文件，不从旧工程素材库随意挑选。

用户明确要求继续某个既有工程时，按其提供的名称、路径或 ID 精确定位并核实；不能唯一定位时只补问目标，不猜测。此分支仍须先完成下一步的时间线复制。

### 3. 导入与复制时间线（任何剪辑前完成）

- 原始视频：导入已核实的目标工程，建立「原始素材」时间线，再复制为「包装版」。
- 已有粗剪：只读取用户明确指定的源工程和源时间线，通过 ChatCut 实际支持的复制或导入方式放入新工程，保留一份粗剪基线，再创建「包装版」副本。不能跨工程复制时，说明缺失能力并索取可导入材料，不自动回到源工程编辑。
- 用户明确要求在既有工程继续时，也先复制其指定时间线为「包装版」，不修改源时间线。
- 核实副本与基线是两个独立时间线，记录副本 ID 或唯一标识，并将副本设为当前目标。后续抠像、精剪、字幕和包装都只操作该副本。

### 4. 人物抠像与 A-roll 精剪（仅在包装版副本中）

**需要抠像时，先对副本中的整条原始视频抠像，再精剪分段，子段继承抠像结果。用户不需要抠像时直接进行精剪。**

1. 对原始整条视频 clip 添加 1 次 AI Portrait Cutout，等待处理完成（保持 ChatCut 桌面前台；查询分批，每次 ≤20 个 effect ID）。
2. 抠像完成后进行 A-roll 精剪分段。
3. 验证继承：抽帧确认所有子段背景透明；若有子段未继承 → 降级：仅对缺失段补挂效果，每批 ≤10 个。

输入已是粗剪时间线时：对每段挂 AI Portrait Cutout，分批挂载（每批 ≤10 个，完成后再挂下一批），全程保持 ChatCut 前台。

原始视频的精剪按 talking-head-guide 的 A-roll 流程：转写 → clean_script 机械清理 → 语义精剪 → apply_script。目标是去口癖、停顿和重录，保留完整语义单元与自然节奏。输入已经是粗剪时间线时跳过此精剪流程。

**卡住处理 SOP**：效果处理超过约 20 分钟无进展时——① 分批查询状态定位（每次 ≤20 ID）② 确认 ChatCut 桌面在前台（macOS App Nap 会挂起后台处理）③ 保存本次工程后重启 ChatCut，重新核实目标工程和包装版时间线 ④ 仍无效则仅对该副本中已定位的失败效果删除并重新挂载。

### 5. 字幕

- 开启字幕，默认使用「青松手写体（Qingsong Shouxie Ti）」（ChatCut 字体库内置；如用户另有指定则按用户指定）。
- 字号：72px，字间距：1，白色字体，黑色阴影 85% 强度，无描边。
- 关键词红色高亮（`#FF3333`，fontWeight:700）：根据内容语义判断，典型如技术术语、产品名、核心卖点词。
- 字幕位置参考背景底图模版底部安全区。

### 6. 画面层搭建（轨道顺序从下到上）

| 轨道 | 内容 | 说明 |
|---|---|---|
| V1 背景 | 背景图（用户提供或默认米白） | 全程铺底，left:0 top:0，铺满画布 |
| V2 人声（anchor） | 口播视频 | 已含 AI 抠像；按画幅布局放置（竖版 height:1440 top:480） |
| V3 B-roll | 各章节配图 | 按画幅布局，与人物同时可见、不遮挡人物 |
| V4 MG标题 | 章节标题 MG | 每章节开场，时长2秒，极简细线风格 |
| V5 章节进度条 | 进度条 MG | 全程，调用已沉淀进度条 Skill |
| A1 背景音乐（follower） | BGM | 仅当输入澄清选择要 BGM；V2 设 anchor，A1 设 follower 自动闪避 |

### 7. B-roll 配图

- 根据语义在各章节关键时刻放置配图，每处约 3–4 秒。
- 尺寸位置按画幅布局：竖版 width:763，top:250，left:158（与底图模版素材区对齐）；横版同比例换算。
- 与人物同时可见，不遮挡人物。

### 8. 章节 MG 标题

- 每进入新章节时叠加标题，时长约 2 秒，放在 V4 轨道。
- 极简细线风格：深色文字、细横线装饰、淡入淡出。
- 首个 MG 经确认后，后续章节复用其视觉系统，保持一致。

### 9. 章节进度条

- 调用「进度条 Skills」制作或复用章节进度条。
- 确认章节帧属性与当前时间线章节起始帧一致。
- 进度条覆盖全程，放在最高视频轨道，并根据实际字幕和安全区确定位置。

### 10. 背景音乐（可选）

- 仅当输入澄清中用户选择要 BGM。
- 轻快、博主风、无人声、acoustic + electronic 混合，不分散注意力。
- 覆盖视频内容全长，超出部分截断，添加淡入淡出各 2 秒。
- 人声轨设为 anchor，音乐轨设为 follower，自动在人声下方闪避。

### 11. 验证

- 核对当前应用、工程和包装版时间线与记录一致，源工程、基线时间线均未被修改。
- 结构核对为主：preview_timeline timeline 视图检查轨道、条目数、图层位置（快）。
- 抽帧为辅：每章首尾各 1 帧 + 最后一帧，检查抠像、B-roll、章节标题、进度条、字幕。
- 不高频调用 viewer；全部合格后再导出。

### 12. 导出

- 从已核实的目标工程和包装版时间线导出，不使用未经核实的当前活动时间线。
- 按确认的画幅导出（竖版 1080×1920 / 横版 1920×1080）。
- 导出后抽帧核验，确认后交付文件。

## 关键规则

- 不删减内容：包装层只在 A-roll 之上叠加，不裁剪口播内容。
- 先抠像后精剪（输入为原始视频时）；子段未继承则降级补挂，每批 ≤10。
- 进度条帧数对齐：章节帧必须与实际章节起始帧一致。
- BGM 不超过视频结尾：音乐不能延长时间线。
- 抠像需要等待：AI 处理期间不要反复重试；保持 ChatCut 前台。
- 效果状态查询每次 ≤20 个 ID；卡住按 SOP 处理。
- B-roll 尺寸统一：所有配图沿用同一位置和尺寸系统。

## 完成检查

- [ ] 使用 ChatCut（或用户明确指定的编辑器），没有自动切换到剪映。
- [ ] 新任务已新建并核实独立工程；复用旧工程仅限用户明确指定。
- [ ] 目标工程、包装版时间线及素材来源有记录，所有写入与导出均指向该目标。
- [ ] 在抠像与精剪前已创建独立包装版副本，旧工程及源时间线未被误改。
- [ ] 输入澄清已确认（画幅/抠图/背景/BGM）。
- [ ] 字幕青松手写体 72px、关键词高亮覆盖核心词。
- [ ] 抠像完成且所有段背景透明（无未继承段）。
- [ ] 进度条章节帧数与时间线一致。
- [ ] B-roll 所有图片位置一致，没有偏移。
- [ ] 章节 MG 标题视觉风格一致。
- [ ] 背景音乐（如有）在人声段落自动压低。
- [ ] 原始时间线未被修改，副本独立可编辑。
- [ ] 按确认画幅导出，成片抽帧核验通过。
